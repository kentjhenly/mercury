{
  "name": "mercury-email-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Email Worker that forwards inbound applicant mail to Mercury's ingest API.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "postal-mime": "^2.4.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^3.80.0"
  }
}
